<% if (nominations.length) { %>Lunch nominations:
<% nominations.forEach(function(nomination) { %>  + <%= nomination.where %> (<%= nomination.user %>)
<% nomination.votes.forEach(function(user) { %>    - <%= user %>
<% })}) %><% } else { %>No Lunch nominations<% } %>
